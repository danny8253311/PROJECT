@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@section Styles
{
    <style>

    </style>
}
 
<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User) && ((User.IsInRole("Admin") || (User.IsInRole("Boss") || (User.IsInRole("Manager")) || (User.IsInRole("Employee"))))))
    {
        <div class=" d-flex align-items-center justify-content-end">
            <div class="ms-3 ibox d-flex align-items-center">
                <a href="wishlist.html"
                   class="position-relative d-flex justify-content-center align-items-center">
                    <span class="icon me-3"><i class="flaticon-heart"></i> </span>
                    @*<span class="count bggrn color-white position-absolute"></span>*@
                </a>
            </div>
            <a href="#offcanvasRight" data-bs-toggle="offcanvas"
               class="ms-3 ibox d-flex align-items-center">
                <div class="position-relative d-flex justify-content-center align-items-center">
                    <span class="icon me-3"><i class="fa-solid fa-cart-arrow-down"></i> </span>
                    @*<span class="count color-white position-absolute"></span>*@
                </div>

            </a>
        </div>
    <li class="nav-item">
        <a  class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index_front" title="Manage">@User.Identity?.Name!</a>
    </li>
    <li class="nav-item">
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index_front", "Home", new { area = "" })">
            <button  type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>
    </li>
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage"><i class="fa-solid fa-gear"></i></a>
        </li>
}
else if (SignInManager.IsSignedIn(User)||(User.IsInRole("Member")))
{
        <div class=" d-flex align-items-center justify-content-end">
            <div class="ms-3 ibox d-flex align-items-center">
                <a href="wishlist.html"
                   class="position-relative d-flex justify-content-center align-items-center">
                    <span class="icon me-3"><i class="flaticon-heart"></i> </span>
                    @*<span class="count bggrn color-white position-absolute"></span>*@
                </a>
            </div>
            <a href="#offcanvasRight" data-bs-toggle="offcanvas"
               class="ms-3 ibox d-flex align-items-center">
                <div class="position-relative d-flex justify-content-center align-items-center">
                    <span class="icon me-3"><i class="fa-solid fa-cart-arrow-down"></i> </span>
                    @*<span class="count color-white position-absolute"></span>*@
                </div>
            </a>
            <a class="nav-link hover-base text-capitalize font-16" asp-area="" asp-controller="Home" asp-action="GPT">
            <div class="position-relative d-flex justify-content-center align-items-center">          
                <span class="icon me-3"><i style="font-size:28px;margin-left:4%" class="fa-solid fa-robot"></i> </span>
                @*<span class="count color-white position-absolute"></span>*@
            </div>
            </a>
        </div>

    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index_front" title="Manage">@User.Identity?.Name!</a>
    </li>
    <li class="nav-item">
        <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index_front", "Home", new { area = "" })">
            <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>
        </li>
    }
    else
    {
        <div class="ms-3 ibox d-flex align-items-center">
            <a asp-area="Identity" asp-page="/Account/Login_front"
               class="position-relative d-flex justify-content-center align-items-center">
                <span class="icon me-3">
                    <i class="fa-solid fa-arrow-right-to-bracket"
                       title="登入"></i>
                </span>
            </a>
        </div>
    }
</ul>

@section Scripts
    {
    <script>

    </script>
}
